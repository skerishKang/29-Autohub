import { logger } from '../../config/logger';

// 텔레그램 봇 초기화 (MVP 단계에서는 스텁)
export async function initializeTelegramBot(): Promise<void> {
    logger.info('Telegram 봇 초기화는 아직 구현되지 않았습니다. (스텁)');
}
